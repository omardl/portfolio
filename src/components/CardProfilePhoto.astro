---
import Badge from './Badge.astro'

interface Props {
    img: string;
}

const { img } = Astro.props;
---

<div class="shadow-grid-mini hover:shadow-none dark:shadow-grid hover:dark:shadow-grid-hover col-start-1 row-start-2 row-span-2 rounded-3xl transition-transform border-[#212121] duration-300 hover:scale-105" style={`background-image: url('${img}'); background-size:cover;`} >