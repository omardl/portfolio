---
import Badge from '../components/Badge.astro'

interface Props {
    img: string;
}

const { img } = Astro.props;
---

<div class="grid-shadow test col-start-1 row-start-2 row-span-2 rounded-3xl transition-transform duration-300 hover:scale-105" style={`background-image: url('${img}'); background-size:cover;`} >